/**
 * @module AppRouter
 * @author Vladimir Rodkin <mail@vovanr.com>
 */

define([
    'jquery',
    'lodash',
    'backbone',
], function (
    $,
    _,
    Backbone
) {

    'use strict';

    /**
     * @alias module:AppRouter
     */
    var AppRouter = Backbone.Router.extend({
        /**
         */
        initialize: function () {
        },
    });

    return AppRouter;

});
