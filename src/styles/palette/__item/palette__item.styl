.b-palette__item {
    position: relative;
    float: left;
    width: 50px;
    height: 50px;
    border-radius: 0;
    color: #fff;
    transition: border-radius 0.5s ease 0s,
                transform 0.25s ease 0s;
    transform: scale(1);
}
