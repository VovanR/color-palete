.b-palette-selected-color__item {
    overflow: hidden;
    padding: 10px;
    border-radius: 4px;
    background-color: #ccc;
    box-shadow: inset 0 0 0 1px,
                inset 0 0 0 2px rgba(255, 255, 255, 0.5);
    color: #ccc;

    + .b-palette-selected-color__item {
        margin-top: 2px;
    }
}
